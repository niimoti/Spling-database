CREATE DATABASE sample;
  USE sample;

    CREATE TABLE sample_table (
    id VARCHAR(8) PRIMARY KEY,
    name VARCHAR(64)
  );

CREATE USER 'root'@'localhost' IDENTIFIED BY 'password'; 

GRANT ALL PRIVILEGES ON sample.*
   TO root@localhost;

FLUSH PRIVILEGES;